<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="90"
        android:startColor="@color/colorSideNavStart"
        android:centerColor="@color/colorSideNavEnd"
        android:endColor="@color/white"
        android:type="linear" />
</shape>